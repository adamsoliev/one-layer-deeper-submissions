[project]
name = "one-layer-deeper-submissions"
version = "0.1.0"
description = "Submission tracker for the One Layer Deeper competition."
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.3,<2",
]

[tool.uv]
package = false
